<form method="post" action="{{ path('menu_catfood_delete', {'id': menu_catfood.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ menu_catfood.id) }}">
    <button class="btn">Delete</button>
</form>
