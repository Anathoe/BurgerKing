{% extends 'base.html.twig' %}

{% block title %}MenuCatfood{% endblock %}

{% block body %}
    <h1>MenuCatfood</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu_catfood.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('menu_catfood_index') }}">back to list</a>

    <a href="{{ path('menu_catfood_edit', {'id': menu_catfood.id}) }}">edit</a>

    {{ include('menu_catfood/_delete_form.html.twig') }}
{% endblock %}
