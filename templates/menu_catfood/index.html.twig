{% extends 'base.html.twig' %}

{% block title %}MenuCatfood index{% endblock %}

{% block body %}
    <h1>MenuCatfood index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for menu_catfood in menu_catfoods %}
            <tr>
                <td>{{ menu_catfood.id }}</td>
                <td>
                    <a href="{{ path('menu_catfood_show', {'id': menu_catfood.id}) }}">show</a>
                    <a href="{{ path('menu_catfood_edit', {'id': menu_catfood.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('menu_catfood_new') }}">Create new</a>
{% endblock %}
