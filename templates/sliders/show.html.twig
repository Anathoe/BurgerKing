{% extends 'base.html.twig' %}

{% block title %}Sliders{% endblock %}

{% block body %}
    <h1>Sliders</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slider.id }}</td>
            </tr>
            <tr>
                <th>Nom_image</th>
                <td>{{ slider.nomImage }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ slider.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ slider.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sliders_index') }}">back to list</a>

    <a href="{{ path('sliders_edit', {'id': slider.id}) }}">edit</a>

    {{ include('sliders/_delete_form.html.twig') }}
{% endblock %}
